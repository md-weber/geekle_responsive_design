class Insets {
  static const double small = 4.0;
  static const double medium = 8.0;
  static const double large = 12.0;
  static const double xLarge = 16.0;
}

class AnimationSpeed {
  static const Duration fastest = Duration(milliseconds: 200);
  static const Duration fast = Duration(milliseconds: 300);
  static const Duration medium = Duration(milliseconds: 600);
  static const Duration slow = Duration(milliseconds: 1200);
  static const Duration slowest = Duration(milliseconds: 1600);
}